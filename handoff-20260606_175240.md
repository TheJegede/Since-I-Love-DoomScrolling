# Handoff — 2026-06-06

## 1. Next Session Focus
Finish **sub-project D** (Vercel dashboard) — only two steps remain:
1. **Dashboard local test** — point the Vite frontend at Supabase and confirm reels load from it.
2. **Vercel deploy** — host the frontend, reading Supabase directly.

Plan to follow: `docs/superpowers/plans/2026-06-06-vercel-dashboard.md` — Tasks 1–4 are DONE (RLS, supabase-js client, repointed reads, placeholders). Only **Task 3 Step 7 (local test)** and **Task 5 (Vercel deploy)** are left.

### ⚠️ Do this FIRST (the env gotcha)
The frontend reads env from **`frontend/`**, not the repo root, and only `VITE_`-prefixed vars. The user's keys live in the **root `.env`** as `SUPABASE_URL` / `SUPABASE_SERVICE_KEY` (backend names + the **secret** key — must NOT go to the browser). So:
- Create **`frontend/.env.local`** (gitignored) with:
  ```
  VITE_SUPABASE_URL=https://jljhgvrmyfntiqmizjfg.supabase.co
  VITE_SUPABASE_ANON_KEY=<the sb_publishable_ key>
  ```
- The **publishable** (`sb_publishable_…`) key is the anon key for the browser. The root `.env` does NOT contain it (only the secret key) — ask the user for the publishable key, or copy it from Supabase → Settings → API.
- Offer to verify locally: with `frontend/.env.local` set, do a headless anon read against Supabase REST (or `npm run dev`) before deploying.

For Vercel (Task 5): project root = `frontend`, set `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` in Vercel env, leave `VITE_API_URL` unset in prod (hides Recompute, which needs the local backend).

## 2. Session Work (this session only)
Branch: **`feat/supabase-hybrid`** (NOT merged to main yet). ~12 commits. All backend tests green (`python backend/verify_pipeline.py`, 15 tests). Done this session:
- **Clustering scale fix:** `cluster_topics_with_llm` now chunks (`CLUSTER_CHUNK_SIZE=50`, `CLUSTER_CHUNK_DELAY=30s`) with index-shrink + a `_merge_cluster_names` consolidation pass (fixes Groq 6000 TPM 413 on ~350 reels). Tests: `test_cluster_chunking`, `test_cluster_merge_pass`.
- **Delete feature:** `DELETE /reels/{id}` + `db.delete_reel`; trash buttons on cards/table/modal in `App.jsx` (+ `.delete-btn` CSS).
- **Data cleanup:** removed 24 test-junk rows (url IS NULL). Final = **343 real reels**.
- **Test-pollution bug fixed:** `verify_pipeline.py` writes to the REAL DB; old `test_recompute_clusters_mock` had overwritten all clusters to "Other". All DB-touching tests now mock `db.*`.
- **Sub-project A (Supabase migration) — DONE + verified live:** `process_reel_url` extracted; `backend/db.py` data layer; `main.py` fully off SQLite → `db.*`; `migrate_sqlite_to_supabase.py` ran (343 rows migrated, 0 failed); recompute re-run on Supabase → 40 clusters; URL cache hit verified (0.34s).
- **Sub-project B (worker) — DONE + verified live:** `db.claim_next_pending/update_reel_result/mark_failed`; `process_pending_reel`/`worker_tick`/`_worker_loop` + startup hook gated by `ENABLE_WORKER`. Live test: a `pending` row went pending→processing→done.
- **Sub-project C — user completed:** RLS enabled (insert-only anon + select anon policies) and the iPhone Shortcut built. (User confirmed.)
- **Sub-project D — CODE done (this session):** `frontend/src/supabaseClient.js`, `frontend/.env.example`, repointed `fetchReels` to Supabase + client-side `computeClusters`, queued/processing placeholders, Recompute gated on `!isWakingUp`, live-refresh poll. `npm run build` passes; lint shows only 6 PRE-EXISTING errors (unused React/CheckCircle2/FileTextIcon + 3 react-hooks/immutability) — none new.

## 3. Blockers & Gotchas
- **ffmpeg must be on PATH** for the backend/worker process or yt-dlp fails ("ffprobe and ffmpeg not found") → reels marked `failed`. Located at `C:\Users\jeged\AppData\Local\Microsoft\WinGet\Packages\Gyan.FFmpeg_*\ffmpeg-*-full_build\bin`. The user's `run_local.ps1` shell has it; tool-launched uvicorn may not.
- **Supabase new keys:** `sb_publishable_` (anon/frontend) + `sb_secret_` (service/backend). Needs `supabase-py>=2.31.0` AND `websockets>=13,<16` (else `ModuleNotFoundError: websockets.asyncio`). Both pinned in `backend/requirements.txt`.
- **DNS:** machine resolver SERVFAILs on `*.supabase.co`; user set Wi-Fi DNS to 1.1.1.1/8.8.8.8. If connectivity fails again, recheck DNS.
- **Vite env**: see Section 1 — root `.env` is NOT read by Vite; needs `frontend/.env.local` with `VITE_` names + publishable key.
- **Backend may be running in background** from this session (a tool-launched uvicorn on :8000 with worker enabled). The next session should confirm/own it; the user normally runs `backend/run_local.ps1`.
- Recompute lost single-transaction rollback (now per-row `db.set_cluster`) — acceptable, clusters advisory.

## 4. Artifact References
- `CLAUDE.md` — architecture, commands, known behaviors (note: storage is now Supabase, not SQLite — CLAUDE.md still says SQLite; update if time permits).
- Specs: `docs/superpowers/specs/2026-06-06-share-to-app-hybrid-design.md`
- Plans: `docs/superpowers/plans/2026-06-06-vercel-dashboard.md` (the one to resume), plus `…-supabase-migration.md`, `…-worker-poll-loop.md`, `…-iphone-shortcut.md`, `…-bulk-import.md`.
- Memory: `.claude/projects/C--Users-jeged-Downloads-Transcriber/memory/` — esp. `hybrid-architecture-direction.md`, `ffmpeg-path-and-supabase-setup.md`, `verify-pipeline-uses-real-db.md`.
- Supabase project ref: `jljhgvrmyfntiqmizjfg` (org "Taiwo's Org", us-east-1).

## 5. Suggested Skills for Next Session
- `/executing-plans` (or `/subagent-driven-development`) — resume `2026-06-06-vercel-dashboard.md` Tasks 3.7 + 5.
- `/finishing-a-development-branch` — once D verified, decide merge of `feat/supabase-hybrid` → main.
- `/verify` or `/run` — to launch the dashboard and confirm it reads Supabase before deploying.
