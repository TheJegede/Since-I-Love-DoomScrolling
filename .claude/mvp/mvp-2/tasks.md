# MVP 2: Web Dashboard (React + Vite)

Status: in_progress (built, polish/verify pending)

- [x] Vite + React 19 app, single `App.jsx` component
- [x] API base resolution (`VITE_API_URL` / localhost:8000 / same-origin)
- [x] URL input + submit → `/extract/url`
- [x] File upload fallback → `/extract/file`
- [x] Text paste fallback → `/extract/text`
- [x] `/health` poll → backend warm-up state
- [x] Card grid + search → `/reels`
- [ ] Detail modal: copy action items, view raw transcript (PRD 3.4)
- [ ] Real-time stage status log (scrape/transcribe/summarize/save)
- [ ] Basic access gate (token/password) for public deploy (PRD 3.4)
- [ ] Mobile responsiveness pass
